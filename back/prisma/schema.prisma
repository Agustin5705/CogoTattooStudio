// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DATABASE_URL_UNPOOLED")
}

model Contact {
  id          Int      @id @default(autoincrement())
  fullName    String
  wpp         String
  link        String?
  presupuesto Float?
  fecha       DateTime?  // <-- Agrega este campo
  createdAt   DateTime @default(now())
}

model GalleryImage {
  id          String   @id @default(uuid()) @db.Uuid
  publicId    String   @unique // ID único que Cloudinary usa para borrar
  secureUrl   String   @unique // La URL de la imagen (lo que mostrarás)
  description String?  // Campo opcional para una descripción o etiqueta
  createdAt   DateTime @default(now())
}